# back/.env.docker - usado por docker-compose (DB dentro de la misma red)
NODE_ENV=development
DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=admin
DB_NAME=shift_aplication

# Si tu código soporta DATABASE_URL:
# DATABASE_URL=postgres://postgres:admin@db:5432/shift_aplication
